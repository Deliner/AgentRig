purpose: Define rule identities, metrics, parameters and architecture settings independently of language handlers
files:
  architecture.yaml: Rule definition responsibilities and public boundary
  kind.rs: Stable rule identities and their serialized names
  descriptor.rs: Rule metrics, parameter contracts, defaults and repair skill bindings
  architecture.rs: Architecture rule settings, schema validation and capability description
directories: {}
allow: []
deny: []
public: [kind.rs, descriptor.rs, architecture.rs]
