purpose: Validate review answers against assigned requirements, identities and verdict rules
files:
  architecture.yaml: Response responsibilities and public validation boundary
  mod.rs: Response schema validation, identity checks, verdict rules and file validation entry
  contract.rs: Requirement schema, reviewer assignments and contract validation
  response.json: Machine-readable answer schema consumed by reviewers and the validator
  tests.rs: Answer identity, requirement coverage, verdict and unsafe file behavior tests
directories: {}
allow:
  - tooling/worker/review/src/artifacts/mod.rs
deny: []
public: [mod.rs, contract.rs]
