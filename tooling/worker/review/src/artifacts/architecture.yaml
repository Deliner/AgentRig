purpose: Read, fingerprint and atomically persist shared file artifacts for execution and verification
files:
  architecture.yaml: Artifact byte responsibilities and public boundary
  mod.rs: Shared regular-file reading with caller limits and SHA-256 content fingerprints
  json.rs: Atomic JSON serialization and replacement after file synchronization
  tests.rs: Bounded reads, fingerprints and JSON replacement and failure behavior
directories: {}
allow: []
deny: []
public: [mod.rs, json.rs]
