# I002

## Predicate

Every invariant links an existing test carrying its invariant marker.

## Oracle

Configured target `tooling/worker/src/scaffold/testing/test_delivery.py::test_independent_project_delivery` verifies this behavior through the native runtime. D020 updates the implementation and current ownership while preserving the predicate except its explicit revisions to I001 and I003.
