# I006 — VAC commit boundary

## Predicate

Under [D011](../Decisions/011.md), failed staged checks reject commits; repeated edits and corrections remain available, and unrelated unstaged work is excluded. D011 replaces the former edit-checkpoint predicate.

## Oracle

[test_vac_commit_boundary](../../tooling/tests/test_commit_checkpoint.py) exercises the real pre-commit hook with a staged-content oracle, repeated edits, a rejected commit, correction, and unrelated unstaged work. The remaining tests verify that the retired adapter is inert and no longer registered while complexity reminders remain registered.
