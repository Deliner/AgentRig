# I006

## Predicate

Failed staged checks reject commits; repeated edits and corrections remain available, and unrelated unstaged work is excluded.

## Oracle

Configured target `tooling/worker/src/scaffold/git/tests/test_git.py::test_staged_commit_preserves_unstaged_work` verifies this behavior through the native runtime. D020 updates the implementation and current ownership while preserving the predicate except its explicit revisions to I001 and I003.
