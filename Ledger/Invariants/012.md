# I012

## Predicate

Ledger edits receive matching skill guidance without a new edit denial, preserving existing guard and complexity decisions.

## Oracle

Configured target `tooling/tests/native/scaffold/test_hooks.py::test_ledger_edit_guidance` verifies this behavior through the native runtime. D020 updates the implementation and current ownership while preserving the predicate except its explicit revisions to I001 and I003.
