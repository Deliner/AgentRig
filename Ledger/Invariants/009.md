# I009

## Predicate

The Plan accepts idle and paused delivery, allows at most one active feature, and rejects malformed contracts or invalid prerequisites.

## Oracle

Configured target `tooling/worker/src/scaffold/memory/tests/test_plan.py::test_plan_delivery_contract` verifies this behavior through the native runtime. D020 updates the implementation and current ownership while preserving the predicate except its explicit revisions to I001 and I003.
