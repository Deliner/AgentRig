# I019

## Predicate

Resume reports real merge and rebase conflicts without modifying repository contents.

## Oracle

test_resume_observes_real_conflict in tooling/worker/src/scaffold/memory/tests/test_resume.py executes the native worker in an independent Git repository. D022 defines the recovery contract.
