# I007

## Predicate

Complexity reminders use the configured attention and full-refresh intervals.

## Oracle

Configured target `tooling/tests/native/scaffold/test_reminders.py::test_portable_reminder_schedule_retry_and_compaction` verifies this behavior through the native runtime. D020 updates the implementation and current ownership while preserving the predicate except its explicit revisions to I001 and I003.
