# I005

## Predicate

Agent shell tools accept only one top-level invocation of a catalogued Just recipe.

## Oracle

Configured target `tooling/worker/src/hooks/tests/test_hooks.py::test_shell_guard_rejects_bypass` verifies this behavior through the native runtime. D020 updates the implementation and current ownership while preserving the predicate except its explicit revisions to I001 and I003.
