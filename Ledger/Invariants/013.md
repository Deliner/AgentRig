# I013 — Native hook compatibility

## Predicate

Registered native hooks preserve Ledger guidance responses, command denial behavior, and complexity reminder transitions and on-disk state for the established event protocol.

## Oracle

[test_native_hook_contract](../../tooling/tests/native/test_hooks.py) directly tests native routing and guidance. Companion native tests exercise transcript offsets, attention/full-refresh thresholds, retry behavior, compaction, storage failures and command/Git guards. Under D019, no executable Python reference is imported.
