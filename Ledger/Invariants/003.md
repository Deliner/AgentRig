# I003

## Predicate

Committed decision identities, statements and details are immutable; current application links point to existing marked owners.

## Oracle

Configured target `tooling/tests/native/scaffold/test_memory.py::test_committed_decisions_checked_against_staged_memory` verifies this behavior through the native runtime. D020 updates the implementation and current ownership while preserving the predicate except its explicit revisions to I001 and I003.
