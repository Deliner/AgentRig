# I010

## Predicate

Direct master commits fail; integration uses current master, preserves feature commits, and retains the merged feature reference.

## Oracle

Configured target `tooling/tests/native/scaffold/test_git.py::test_divergent_rebase_preserves_merge_history` verifies this behavior through the native runtime. D020 updates the implementation and current ownership while preserving the predicate except its explicit revisions to I001 and I003.
