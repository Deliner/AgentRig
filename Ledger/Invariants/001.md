# I001

## Predicate

Every linked Rust, Python or shell decision application exists and carries its decision marker.

## Oracle

Configured target `tooling/tests/native/scaffold/test_memory.py::test_decision_source_markers` verifies this behavior through the native runtime. D020 updates the implementation and current ownership while preserving the predicate except its explicit revisions to I001 and I003.
