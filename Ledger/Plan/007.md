# P007 — Extensible version control support

## Feature

Support AgentRig delivery with multiple version control systems and an extension boundary usable by private corporate implementations.

## User capability

The user selects a supported VCS for a consumer project, retains verified changes and recovery, and runs isolated review and code delegation against exact revisions. An organization can supply its own VCS integration without publishing proprietary implementation code or modifying each AgentRig workflow.

## Acceptance

- Git remains supported and at least one other real VCS is implemented and demonstrated in an independent consumer. The user delegates selection to the implementing agent: choose an available system with the simplest sufficient integration to exercise the extensible architecture, and record that choice. No further user selection is required.
- A documented integration boundary owns VCS-specific operations used by configuration/setup, revision and change inspection, snapshots, delivery checks, integration, recovery, review and delegated code results. Existing Git assumptions are not left hidden in callers of the new backend.
- Each backend represents its actual semantics. Unsupported operations are explicit actionable capability errors; Git staging or branch concepts are not presented as available where they do not exist. Equivalent supported delivery preserves the exact checked changes, unrelated work and recoverable integration context.
- A consumer can configure a private implementation through the documented extension contract. A separate example exercises this boundary without changing AgentRig's workflow logic or publishing corporate code. Validate data crossing the extension boundary and retain existing snapshot/result restrictions.
- Tests exercise real repositories for Git and the selected second VCS: changes, deletions/renames where supported, exact revision snapshots, failed checks, isolation, recovery and successful integration. Review and code delegation preserve the original workspace and return inspectable, verified results for both.
- Setup, diagnostics, documentation and configuration describe actual backend capabilities. Existing Git consumers remain usable; configured gates and independent consumer acceptance pass.

## Delivery

Requested by the user after P005. The user confirmed VCS support second in delivery priority and authorized the agent to choose the simplest available additional system. The purpose is to prove the extensible architecture. Arc was mentioned as a possible future privately supplied integration; implementing Arc now is outside this delivery. Keep the private extension capability required above without assuming access to Arc. Repository hosting such as GitHub is distinct from the VCS. Full implementation is authorized by the active P006 → P007 → P008 goal.

P007 starts on feature/vcs-backends from 0c9cc39 after verified P006 integration. Mercurial is the selected second VCS. Only Git was initially installed; Mercurial 7.2.4 is available through the existing locked uv test environment. It exercises a real independent revision store without Git staging, while avoiding an additional system package toolchain for tests. Git-compatible storage alone would not demonstrate these differences; Arc remains unavailable and outside scope. Consumers using Mercurial will need its executable; native Git consumers retain their existing requirements.

Implementation acceptance is verified on feature/vcs-backends through 175e6fe. Its full staged gate passed with 567 Python and 138 Rust tests, including formatting, Clippy, lint, memory, Python type/static checks and the review crate. This completion records implementation and consumer acceptance; integration into the configured base remains the next required action, not a completed fact.

- Native Git delivery remains covered by independent Python/Rust consumers and divergent integration tests. Native Mercurial and a copied private adapter complete installed consumer delivery through generated Just recipes: bootstrap integration, feature creation, guarded commits, failed integration gate, recovery, exact merge parents, retained feature and subsequent feature creation. The base is committed before hook installation. The installed runtime-ignore defect found by this acceptance was repaired in 9d98a07 without weakening clean-tree checks or read isolation.
- The shared Backend/Source owner selects native Git, native Mercurial or a literal-argv protocol-v1 adapter for setup generation/registration, observations, immutable reads, snapshots, checks, integration, recovery, review and delegation. The protocol and independent standard-library Mercurial example are documented in tooling/worker/review/EXTERNAL_VCS.md; corporate source is neither needed nor published.
- Actual semantics are retained: Mercurial named branches and native merge parents, Git index/rebase behavior, exact opaque private revision IDs and explicit unsupported-operation errors. Staging tests preserve invalid unstaged Git configuration, reject private index selection before adapter execution and prevent saved evidence from falling back to nearby native metadata. Native index preparation is explicitly separate because staged configuration is unavailable before export.
- Real repository tests cover binary/mode/symlink handling, changes, renames/deletions, ambiguous revisions, fixed exports despite dirty worktrees, failed checks, mutated exports, native conflicts and recovery. External-protocol tests reject malformed data, unsafe paths, invalid versions/types and false write success; read processes cannot write the source repository. Snapshot visibility and private-material restrictions remain enforced.
- Review tests execute failing review, repair and successful re-review against real native/private Mercurial revisions, retaining the base and original dirty workspace. Delegation tests run read/artifacts/code modes, retain verified reports and patches, preserve unrelated work, exercise failed code checks and import the returned patch into a separate Mercurial base checkout. Test critics and delegates establish VCS routing and result contracts; real harness-client acceptance belongs to P008. The patch builder's internal Git workspace remains explicit.
- Setup, preview, doctor, repeated registration and configuration update/rollback are verified for native and private consumers, including custom paths, local conflicts, existing settings and exact rollback. Root .hgignore includes managed runtime rules for isolated reads while retaining user rules. SCAFFOLD.md and the worker/protocol guides describe the implemented capabilities and limits. The Git-only 0.2.0 to 0.3.0 release transition is not presented as a generic VCS workflow.

P008 remains pending until this feature is integrated. The installed development pin remains unchanged while the candidate is accepted through the configured gate.
