# P020 — Единый каталог компонентов и конструктор окружений

## Feature

[B006](../Backlog/006.md), исходный контракт `30bf0e08faa6053d77b7a4370168ec008c9e3bfb`
с согласованными в чате 2026-09-11 уточнениями К10 и границы К8/К11.
Уточнения сохранены в B006; их ревизия определяется историей этой карточки в Git.
Выбран третьим 2026-09-11.

## User capability

Пользователь собирает разные окружения из общих компонентов, видит их состав и управляет обновлениями. Агент получает подготовленные инструменты и ограничения без повторной ручной сборки обвязки.

## Acceptance

Исходная карточка **B006 / К1–К11**, включая её границы и приёмку.

## Delivery

Не начата; pending. Вопрос К10 закрыт пользователем: известный путь проверяется
до правки; для непрозрачной команды конфигурация сравнивается до и после.
Изменение без действующего допуска требует чтения skill и успешной проверки
конфигурации перед продолжением обычной работы. Восстановление остаётся доступным.
Для непрозрачной команды это обнаружение после выполнения, а не предотвращение.

К4/К10 используют общую координацию [B002](../Backlog/002.md), выбранную позднее
как [P021](021.md). Необходимая для B006 часть общего механизма относится
к её реализации; затем B002 развивает того же владельца. Это не требует
завершить всю B002 раньше B006 и не разрешает временный отдельный координатор.
Сохраняется восстановление [P018](018.md).

Зависимости К8/К11 от B004/B005 разрешены пользователем: конструктор подключает
доступные проверки и показывает фактическое покрытие; используется существующая
инфраструктура независимых потребительских проверок с добавлением недостающих
сценариев конструктора внутри B006. Полные B004/B005 предварительно не требуются.
Семь языков и эвалы правил остаются в B004, общая диагностика и расширенные
сценарии восстановления — в B005. Необходимая приёмка конструктора сохраняется.
Выявленные при планировании вопросы B006 закрыты; реализация и её приёмка ещё не выполнены.
Ветка: feature/backlog-card-review; кода в работе нет.
