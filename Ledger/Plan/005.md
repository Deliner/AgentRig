# P005

## Feature

Stable development and public distribution.

## User capability

Develop AgentRig while its installed development runtime remains usable independently of candidate source changes. Obtain AgentRig and its standalone linter from a public MIT-licensed GitHub repository and prepare independent consumer projects.

## Acceptance

- Working commands and hooks use an explicitly pinned installed runtime; candidate build failures do not replace or disable it.
- Product tests exercise the candidate. Development runtime upgrades are explicit and follow verified acceptance.
- A fresh clone has documented bootstrap, prerequisites and the existing full development gate.
- The public GitHub repository includes MIT, consumer quickstart, configuration/example links and CI. Linux release artifacts include AgentRig, the standalone linter, checksums and release/upgrade notes.
- Publication includes only intended source and documentation; private runtime data, credentials and temporary evidence remain local.

## Delivery

Authorized by the user's confirmation to continue the stable-environment separation and GitHub/MIT publication. P004 is already merged at 034232d. Work proceeds on feature/stable-distribution.

Accepted at 5bb4ff4. [GitHub CI](https://github.com/Deliner/AgentRig/actions/runs/34049505351) passed all 15 configured checks, including 363 Python and 50 Rust tests, then packaged and uploaded the Linux archive. Launcher tests prove that a real candidate build failure leaves installed configuration checks and SessionStart hooks usable. A fresh full clone passed explicit bootstrap, hook registration and configuration validation without changing its tracked tree.

The public [MIT repository](https://github.com/Deliner/AgentRig) and [v0.3.0 release](https://github.com/Deliner/AgentRig/releases/tag/v0.3.0) are available. The release targets exactly 5bb4ff4153a38db2bac9d1e110f0590ce8a597e7 and contains the verified CI artifact, both executables, dependency attribution for 132 crates, revision and migration notes. Archive SHA-256: 95c349e23dd65ee8c72c9cee725164d24b328675e288333aa5bde24634704324. GitHub reports the same asset digest. The downloaded archive independently passed init/setup/doctor/config-check and standalone lint without modifying consumer files; the local receipt is .tmp/publication-tools/github-release-acceptance.json.

Temporary data is ignored and absent from tracked publication contents. Historical secret scanning found only the literal private-key markers used by the snapshot rejection code. Active development remains pinned to verified 034232d; release publication does not implicitly upgrade that installation. Local feature integration remains the final delivery step; reconcile live Git when resuming.
