# P008 — Selectable agent harnesses

## Feature

Prepare client-specific agent environments from AgentRig's shared configuration, retaining the selected client's actual capabilities.

## User capability

The user describes a workflow with models, API/authentication references, skills, hooks, programs and MCP services, selects its agent harness, and receives the concrete configuration and resources needed by that client for project work and delegated execution.

## Acceptance

- Support Codex and Claude Code. Selection is explicit in project and delegate environment declarations.
- Shared declarations are translated into each client's required configuration, paths, resources and supported event/protocol forms through client-specific owners. Existing composition, provenance, setup preview, repeat installation and conflict handling apply to both.
- Model and API/authentication settings are mapped according to the selected client's capabilities. Secrets remain external references. Unsupported settings or hook semantics produce actionable diagnostics rather than silent omission or a claim of equivalent enforcement.
- Configured skills, hooks, programs and MCP servers actually work in each prepared environment. AgentRig's required gates, task contracts, isolation and validation remain enforced independently of model completion claims.
- Review and read/artifact/code delegation use the selected supported client and preserve exact inputs, validated outputs, reports, cancellation, recovery and workspace isolation. Shared runner responsibilities remain shared; client differences stay at their actual boundary.
- Independent consumers demonstrate setup/repeat setup, discovery of skills, actual hook execution, MCP use and real review/delegate runs with both clients. Record tested client versions and limitations. Existing Codex consumers and configured gates continue to pass.

## Delivery

Requested by the user after P005, with Claude Code confirmed alongside Codex and delivery third after P006/P007. Implemented on retained feature/agent-harnesses from 10f1d77. The user's active goal authorizes integration; acceptance is complete and feature-merge remains the final delivery step.

Shared declarations select native project/delegate clients and install model/API references, skills, hooks, programs and MCP settings. Composition, preview, provenance, repeated setup, conflicts, updates and rollback apply to both. Configuration-update acceptance includes changing/removing model/API settings, preserving user preferences and permissions, switching clients and rejecting disabled hooks before writes. The latest complete gate at 3dc57ca passed 607 Python and 145 Rust tests, including native sandbox, task/response validation, cancellation, recovery and exact revision checks.

Independent Linux consumers verified native skill discovery with Codex 0.153.4 and Claude Code 2.1.201. After source package removal, actual Codex gpt-5.6-sol/high runs passed review and read/artifact/code delegation. Claude via OpenRouter passed review run-o7px9A, read run-rSxFAL and artifacts run-dsgPOK with free Nemotron; code run-nOqUpc passed with its inexpensive paid variant after the free quota was exhausted. Evidence includes exact skill/hook/MCP values, an actual MCP tools/call, retained artifacts, a validated applicable patch, unchanged consumer inputs and removed private directories. The first incorrect code edit was rejected by the unchanged content check.

See [portability evidence](../../../tooling/worker/examples/PORTABILITY.md) for run artifacts, versions, gateway settings and provider limitations. Native APIs and model compliance remain client/provider capabilities; unsupported requests and invalid results are not treated as successful execution. The installed development pin is unchanged.
