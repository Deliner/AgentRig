# State

## Focus

Deliver P007, then P008 under the user's active implementation goal; P006 is integrated. Current VAC: check exact native revisions with retained evidence and parent-based memory history.

## Workspace

Branch: feature/vcs-backends

Revision: e7007d8

P006 is integrated at 0c9cc39. Commit e7007d8 adds full native exports after reads, review, delegation, lint, memory history and check freshness; its full gate passed with 443 Python and 112 Rust tests. Resume confirmed a clean branch without merge/rebase operations and a completed successful staged gate for the preceding HEAD. User remarks remain in Ledger/Requests.md. The installed development pin is unchanged.

## Progress

P007 is active. check --revision REV now exports one resolved Git/Mercurial revision and uses its configuration, skills and files. Evidence retains the selected hash, revision-export scope and exact retry; exported input mutation is not a completed proof, and current/full-gate status still requires matching original workspace content and revision. Memory history checks every native parent, including both merge sides, rather than current HEAD or the candidate itself. Staged and working-tree behavior remains supported. Native transaction hooks, shared delivery configuration/setup, outer resume branch/merge facts, private adapters and delivery/recovery operations remain required. The isolated patch builder's internal Git dependency remains explicit. P008 is pending.

## Verification

Fifty-one focused Python gate/history/evidence cases passed, followed by all twenty-four consolidated evidence cases and two added second-merge-parent cases. Eleven real-repository Rust VCS tests pass. Coverage includes selected config/files despite dirty originals, failed checks and executable retries, old revisions, passing full evidence, changed export rejection, root history, immutable decision identity/details/removal and both native merge parents. Rust check, Ruff, mypy and structural lint pass. New cases were consolidated into the existing evidence test owner after the directory-size finding. This VAC's full staged gate is pending; full P007 acceptance remains unproven.

## Blockers

None observed. Only Git was initially on PATH; the locked uv environment now provides hg and the review-test catalog command uses it. No corporate VCS access is needed for the authorized private extension example.

## Next action

Commit exact-revision checking through the staged gate. Add native transaction hooks before Mercurial setup; ensure failed checks reject the transaction and preserve unrelated work, using the actual pending revision. Continue shared delivery configuration, native branch/recovery facts, private adapters and delivery/recovery operations. Verify full independent Git/Mercurial consumer acceptance and private extension behavior before P007 integration, then deliver P008. Preserve the installed development pin while testing candidates.
