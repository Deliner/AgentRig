#!/bin/sh
# DECISION: D015
# DECISION: D011
# DECISION: D010
# DECISION: D005
# DECISION: D004
# DECISION: D003
# DECISION: D002
# DECISION: D020
set -eu
root=$(git rev-parse --show-toplevel)
"$root/tooling/worker/run" guard-commit --root "$root"
exec "$root/tooling/worker/run" check --root "$root" --staged
